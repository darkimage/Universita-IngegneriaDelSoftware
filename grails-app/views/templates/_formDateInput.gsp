<input type="date" class="${css}" id="${id}" onchange="setDate('#${id}')" required="${required}" value="${value}">
<input type="hidden" name="${id}" value="date.struct">
<input type="hidden" name="${id}_month"  id="${id}_month">
<input type="hidden" name="${id}_year" id="${id}_year">
<input type="hidden" name="${id}_day" id="${id}_day">